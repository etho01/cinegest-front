
interface Props {
    entity : number | null;
    cinema : number | null;
    page : string;
}

export const Breadcrumb = ({ entity, cinema, page } : Props) => {
    return (
        <div>Breadcrumb</div>
    )
}