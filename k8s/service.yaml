apiVersion: v1
kind: Service
metadata:
  name: cinegest-front
  namespace: cinegest-site
spec:
  selector:
    app: cinegest-front
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP